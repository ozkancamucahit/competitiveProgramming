#include <bits/stdc++.h>

using namespace std;


typedef pair<int, int> pi;
typedef vector<int> vi;

#define REP(i, a, b) for (int i = a; i < b; ++i)

int main ( void )
{
    int a, b;
    string x;

    // to make input and output more efficient and fix bottleneck
    ios::sync_with_stdio(0);
    cin.tie(0);

    //cin >> a >> b >> x;
    //cout << "output :"<< a << " "<< b << " "<< x<< "\n"; // endl causes flush
    
    //printf( "enter string :" );
    //cout << "enter string :";
    //getline( cin, x );
    //for unknown length of data
    // while ( cin >> x  ) {} 
    //cout << "entered string :"<< x<< "\n";

    // FILE IO
    // freopen("input.txt", "r", stdin);
    // freopen("output.txt", "w", stdout);

    // cin >> x;
    // cout << "entered :"<< x <<"\n";


    // FLOATİNG POINT
    //__uint64_t lol;
    //long double deneme = 0.3 * 3 + 0.1;

    // delta error 1e-9
    //if (abs( a-b ) < 1e-9) {}
    //cout << "size :"<< deneme<<"\n";
    //printf("%.17Lf\n", deneme);

    REP(i, 1, 4){
        cout<<"lol"<<"\n";
    }

    return EXIT_SUCCESS;
}


